> Fase: `<N>` · Documento: roadmap
> Estado: `aguardando operador | aprovado`
> Condutor redigiu: `<AAAA-MM-DD>` · Operador aprovou: `—`
> Base: `docs/scope-brief.md`

# Roadmap — Fase `<N>`

## Como ler

- **Entrega:** o que existe no repositório ao fim do marco.
- **Pronto quando:** critério objetivo, verificável por qualquer pessoa do time.
- **Depende de:** marcos que precisam estar prontos antes.

A unidade de planejamento é o marco, não a data. A ordem dos marcos é obrigatória.
Marcos com o mesmo número podem andar em paralelo.

O DoD geral em `docs/fase N/dod.md` vale por cima de todos os marcos e não se repete aqui.

## Etapa 1 — `<nome da etapa>`

### M0 · `<nome do marco>`
**Entrega:** o que passa a existir, em comportamento observável. Nomes de tabela, rota e
pasta em inglês.
**Pronto quando:** critério que um comando ou uma observação objetiva confirma.
"Funciona bem" não é critério.
**Depende de:** marcos anteriores, ou `nada`.

### M1 · `<nome do marco>`
...

Decisão de produto que sustenta um marco: aponte a seção de `docs/scope-brief.md`. Não
copie a decisão para cá.

Marco que depende de um `[A VALIDAR]` diz isso na primeira linha, antes da entrega.

## Riscos conhecidos e onde estão tratados

| Risco | Marco | Mitigação |
|---|---|---|
| ... | `M?` | ... |

## Fora deste roadmap

O que fica para outra fase. Cada item entra como roadmap próprio quando esta fase fechar.

## Perguntas ao operador

No formato da regra 05, ou `Nenhuma`.
